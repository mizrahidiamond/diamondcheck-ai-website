[build]
  publish = "."

# Redirect www to non-www (preserve SEO link equity)
[[redirects]]
  from = "https://www.diamondcheck-ai.com/*"
  to = "https://diamondcheck-ai.com/:splat"
  status = 301
  force = true

# Serve AASA with correct content-type (required by Apple)
[[headers]]
  for = "/.well-known/apple-app-site-association"
  [headers.values]
    Content-Type = "application/json"
    Cache-Control = "max-age=3600"

# Cache static assets
[[headers]]
  for = "/assets/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"

# Security headers for all pages
[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"
